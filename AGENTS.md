# AGENTS

## 当前阶段工作边界

- 当前所有修改、优化、排障默认限定在 `client/` 目录内。
- 未经明确要求，不修改 `server/`、`.github/`、`scripts/`、根目录配置文件。

## 问题定位原则

- 如果问题现象出现在客户端，优先在 `client/` 内定位原因。
- 即使怀疑与服务端有关，默认只做定位和记录，不直接修改 `server/` 代码。
- 如果确认必须改动 `server/` 才能解决，需要先说明原因并等待确认。

## 修改原则

- 优先做最小范围改动，避免跨端、跨层重构。
- 新增逻辑优先复用 `client` 现有抽象和目录结构。
- 不为“顺手优化”修改与当前目标无关的模块。

## 验证原则

- 完成后检查改动文件是否全部位于 `client/` 下。
- 客户端改动至少执行与 `client` 相关的静态检查或验证。

